WorkoutExercicesTable:
  Type: AWS::DynamoDB::Table
  Properties:
    TableName: ${self:service}_${self:provider.stage}_workout-exercices
    AttributeDefinitions:
      - AttributeName: exerciseId
        AttributeType: S
    KeySchema:
      - AttributeName: exerciseId
        KeyType: HASH
    BillingMode: PAY_PER_REQUEST
    SSESpecification:
      SSEEnabled: true
      SSEType: KMS
      KMSMasterKeyId: !Ref CMKDynamoDB
