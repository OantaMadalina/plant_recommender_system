FootballPlayersTableIAM:
  Effect: Allow
  Action:
    - dynamodb:GetItem
    - dynamodb:BatchGetItem
    - dynamodb:PutItem
    - dynamodb:DeleteItem
    - dynamodb:Scan
    - dynamodb:BatchWriteItem
    - dynamodb:UpdateItem
    - dynamodb:Query
  Resource:
    - arn:aws:dynamodb:${self:provider.region}:#{AWS::AccountId}:table/${self:service}_${self:provider.stage}_football-players-new
    - arn:aws:dynamodb:${self:provider.region}:#{AWS::AccountId}:table/${self:service}_${self:provider.stage}_football-players-new/index/*